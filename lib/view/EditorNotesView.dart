import 'package:flutter/material.dart';

class EditorNotesView extends StatelessWidget {
  const EditorNotesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ismail'),
      ),
    );
  }
}
